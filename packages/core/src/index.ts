/**
 * `@fhirq/core` — the DOM-free engine.
 *
 * Empty by design at M0: the workspace, the type configuration and the
 * architectural lint rules are the deliverable, not the engine. The engine
 * arrives in M1 (vertical slice) and M2 (definition and evaluation).
 */

// M0 placeholder. M1's first act is to delete it (06-roadmap.md §3, M1).
export const CORE_ENTRY_POINT = '@fhirq/core';
