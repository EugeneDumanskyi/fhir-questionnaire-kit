/**
 * `@fhirq/core/view` — the DOM-free presentation model (ADR-0007).
 *
 * A separate entry point from M0 so the budget gate and the import rules have
 * the seam they will police. Populated in M1 and completed in M5.
 */

// M0 placeholder. M1's first act is to delete it (06-roadmap.md §3, M1).
export const VIEW_ENTRY_POINT = '@fhirq/core/view';
