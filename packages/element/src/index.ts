/**
 * `@fhirq/element` — `<fhir-questionnaire>` in shadow DOM.
 *
 * Built through M7, step by step; `@alpha` until it closes.
 */
export { defineQuestionnaireElement, FhirQuestionnaireElement } from './element.js';
