/**
 * `@fhirq/element` — `<fhir-questionnaire>` in shadow DOM.
 *
 * Empty by design at M0. Thin proof in M1, full element in M7.
 */

// M0 placeholder. M1's first act is to delete it (06-roadmap.md §3, M1).
export const ELEMENT_ENTRY_POINT = '@fhirq/element';
