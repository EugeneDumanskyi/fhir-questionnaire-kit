# `cycle`

**Behaviour:** A dependency cycle rejects the load in both modes, naming every linkId, and nothing is evaluated (AC-02.5.1, INV-D-05, M2 AC-2).

Each cycle is one finding naming every member.

**Authored** for the kit, not copied from the HL7 specification. `scenario.json` is read by `packages/core/test/conformance/fixtures.test.ts` (shape: `fixtures/README.md`).
