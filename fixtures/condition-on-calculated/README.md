# `condition-on-calculated`

**Behaviour:** A condition on a calculated item (AC-02.5.5, INV-D-14, ADR-0003) and a binding with no evaluator (INV-D-09).

The evaluator's inputs are invisible to the dependency graph, so acyclicity could not be proven.

**Authored** for the kit, not copied from the HL7 specification. `scenario.json` is read by `packages/core/test/conformance/fixtures.test.ts` (shape: `fixtures/README.md`).
