# `meaningless-condition`

**Behaviour:** An operator that means nothing for the question's type (AC-02.5.3, INV-D-06, plan D3).

A diagnostic in both modes, never a runtime error; the condition evaluates `false`.

**Authored** for the kit, not copied from the HL7 specification. `scenario.json` is read by `packages/core/test/conformance/fixtures.test.ts` (shape: `fixtures/README.md`).
