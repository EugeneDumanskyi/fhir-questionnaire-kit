# `enable-behavior`

**Behaviour:** enableBehavior all and any (AC-02.3.1, AC-02.3.2, INV-S-02).

`enableBehavior` `all` is a conjunction and `any` a disjunction of an item's conditions.

**Authored** for the kit, not copied from the HL7 specification. `scenario.json` is read by `packages/core/test/conformance/fixtures.test.ts` (shape: `fixtures/README.md`).
