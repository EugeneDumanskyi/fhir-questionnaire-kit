# `initial-values`

**Behaviour:** initial[x] and answerOption.initialSelected are ignored with a warning in both modes; items start empty (INV-D-18, plan D4).

Strict rejection would refuse many real forms, so neither mode rejects.

**Authored** for the kit, not copied from the HL7 specification. `scenario.json` is read by `packages/core/test/conformance/fixtures.test.ts` (shape: `fixtures/README.md`).
