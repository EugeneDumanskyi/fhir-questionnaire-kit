# `modifier-extension`

**Behaviour:** A modifier extension the kit does not understand rejects the load in both modes: R4 forbids ignoring it (INV-D-01).

The kit understands no modifier extension.

**Authored** for the kit, not copied from the HL7 specification. `scenario.json` is read by `packages/core/test/conformance/fixtures.test.ts` (shape: `fixtures/README.md`).
